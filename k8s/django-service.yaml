apiVersion: v1
kind: Service
metadata:
  name: django-app
  namespace: jv-web
spec:
  selector:
    app: django-app
  ports:
    - name: http
      port: 80
      targetPort: 8000
    - name: direct
      port: 8000
      targetPort: 8000
  type: ClusterIP
